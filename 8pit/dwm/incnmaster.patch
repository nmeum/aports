diff -upr dwm-6.1.orig/dwm.c dwm-6.1/dwm.c
--- dwm-6.1.orig/dwm.c	2015-11-08 23:39:37.000000000 +0100
+++ dwm-6.1/dwm.c	2017-01-10 16:42:01.229232250 +0100
@@ -981,7 +981,18 @@ grabkeys(void)
 void
 incnmaster(const Arg *arg)
 {
-	selmon->nmaster = MAX(selmon->nmaster + arg->i, 0);
+	int n, nmaster;
+	Client *c;
+
+	n = 0;
+	for (c = selmon->clients; c; c = c->next)
+		if (ISVISIBLE(c)) n++;
+
+	nmaster = MAX(selmon->nmaster + arg->i, 0);
+	if (nmaster > n)
+		nmaster = n;
+
+	selmon->nmaster = nmaster;
 	arrange(selmon);
 }
 
