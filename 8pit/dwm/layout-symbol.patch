diff -upr dwm-6.1.orig/dwm.c dwm-6.1/dwm.c
--- dwm-6.1.orig/dwm.c	2015-11-09 12:05:23.088598473 +0100
+++ dwm-6.1/dwm.c	2015-11-09 12:08:19.191336804 +0100
@@ -724,10 +724,7 @@ drawbar(Monitor *m)
 		           occ & 1 << i, urg & 1 << i);
 		x += w;
 	}
-	w = blw = TEXTW(m->ltsymbol);
-	drw_setscheme(drw, &scheme[SchemeNorm]);
-	drw_text(drw, x, 0, w, bh, m->ltsymbol, 0);
-	x += w;
+	x += blw;
 	xx = x;
 	if (m == selmon) { /* status is only drawn on selected monitor */
 		w = TEXTW(stext);
