#!/sbin/openrc-run

depend() {
	need nldev
	provide dev
	keyword -lxc -vserver
}

start() {
	ebegin "Populating /dev with existing devices through uevents"
	/usr/bin/nltrigger add all &>/dev/null
	eend $?
}
